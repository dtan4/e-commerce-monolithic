app:
  build: .
  environment:
    - RAILS_ENV=production
    - DATABASE_HOST=db
    - DATABASE_PORT=5432
    - DATABASE_USER=postgres
  links:
    - db:db
  ports:
    - 80:3000
db:
  image: postgres:9.4
